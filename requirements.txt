# Este arquivo serve para listar e instalar todas as bibliotecas requeridas para que o projeto funcione corretamente,
# ou seja, antes de rodar o projeto em si deve ser utilizado o seguinte comando para a instalação das depêncdencias
# pip install -r requirements.txt
jupyter>=1.1.1
ipykernel>=6.29.5
pandas>=2.2.3
matplotlib>=3.9.2
seaborn>=0.13.2
openpyxl>=3.1.5
